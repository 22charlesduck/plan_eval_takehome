"""Default configuration settings."""

DEFAULT_CONFIG = {
    "cache_size": 100,
    "log_level": "INFO",
    "output_format": "json",
    "transforms": [],
    "schema": None,
    "max_records": 10000,
    "parallel": False,
}
